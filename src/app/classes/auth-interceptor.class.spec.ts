import { AuthInterceptorClass } from './auth-interceptor.class';

describe('AuthInterceptorClass', () => {
  it('should create an instance', () => {
    expect(new AuthInterceptorClass()).toBeTruthy();
  });
});
